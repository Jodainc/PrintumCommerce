@model IEnumerable<PrintumCommerce.Models.Access.Pro_Lineas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pRO_lN.Linea_Negocio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pRO_lN.Linea_Negocio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Linea }) |
            @Html.ActionLink("Details", "Details", new { id=item.Linea }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Linea })
        </td>
    </tr>
}

</table>
