@model IEnumerable<PrintumCommerce.Models.Access.pRO_hsEG_oTRAiNFO>

@{
    ViewBag.Title = "Index";

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.c16Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.c16iNFO)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.c16Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.c16iNFO)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idCode }) |
                @Html.ActionLink("Details", "Details", new { id=item.idCode }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idCode })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
